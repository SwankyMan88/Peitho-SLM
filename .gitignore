# Regenerated by make_corpus.py
training.txt
heldout.txt

# Encoded-corpus caches, rebuilt automatically from the text above
*.ids.npy

# Full-precision checkpoints. models/*.txt is the portable artifact and is kept:
# it is what the page and standalone.py load, and it is what jsDelivr serves.
*.pt

chat_history.json
__pycache__/
*.pyc
.venv/
